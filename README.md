# Calculadora de Gorjeta

Uma calculadora simples e intuitiva para calcular gorjetas em restaurantes e bares. O usuário digita o valor da conta, escolhe a porcentagem da gorjeta e recebe na tela o valor da gorjeta separado do total a pagar.

---

## Como usar

1. Digite o valor da conta no campo indicado.
2. Selecione a porcentagem da gorjeta (10%, 15% ou 20%).
3. Clique em **Calcular**
4. O valor da gorjeta e o total da conta aparecem na tela.
5. Use o botão **Apagar** para corrigir o valor digitado caractere por caractere.

---

## Funcionalidades

- Cálculo automático da gorjeta e do total da conta.
- Seleção de porcentagem por menu (10%, 15%, 20%).
- Botão de apagar que remove um dígito por vez.
- Validação dos campos: mensagens de erro aparecem individualmente se o usuário tentar calcular sem preencher algum campo.
- Valores formatados em reais (R$).
- Campos limpos automaticamente após o cálculo.

---

## Tecnologias utilizadas

- HTML5
- CSS3
- JavaScript (ES6+)

---

## O que aprendi construindo esse projeto

  Durante o desenvolvimento pratiquei:

- Manipulação do DOM com `getElementById` e `querySelector`.
- Eventos de clique com `addEventListener`.
- Funções e separação de responsabilidades.
- Validação de formulário com mensagens de erro independentes.
- Conversão de texto para número com `Number()`.
- Formatação de valores com `.toFixed()` e `.replace()`.
- Manipulação de strings com `.slice()`.
- Lógica com `if/else`, `return` e variáveis de controle.

---
